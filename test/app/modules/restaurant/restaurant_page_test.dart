import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:uesc_hub/app/modules/restaurant/restaurant_page.dart';

main() {
  testWidgets('RestaurantPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RestaurantPage(title: 'Restaurant')));
    //  final titleFinder = find.text('Restaurant');
    //  expect(titleFinder, findsOneWidget);
  });
}
