import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:uesc_hub/app/modules/sagres/sagres_page.dart';

main() {
  testWidgets('SagresPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SagresPage(title: 'Sagres')));
    //  final titleFinder = find.text('Sagres');
    //  expect(titleFinder, findsOneWidget);
  });
}
